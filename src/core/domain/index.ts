/**
 * Domain Entities
 *
 * Application-wide data models (Entities).
 * These are pure TypeScript interfaces/classes with no dependencies.
 *
 * Example:
 * export interface Task {
 *   id: string;
 *   title: string;
 *   completed: boolean;
 *   createdAt: Date;
 * }
 */

export {};
